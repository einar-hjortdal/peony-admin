import { component } from '@dark-engine/core'

const Edit = component(() => {
  return (
    <>
      <button type='button'>edit</button>
      <dialog>
        stuff
      </dialog>
    </>
  )
})

export default Edit
